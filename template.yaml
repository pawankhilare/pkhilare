{
	"AWSTemplateFormatVersion": "2010-09-09",
	"Description": "S3 bucket creation template",
	"Resources" : {
		"S3Bucket" : {
			"Type" : "AWS::S3::Bucket",
			"DeletionPolicy": "Retain",
			"Properties" : {
				"BucketName" : "cftbucket12"
         }
     }
}
}
