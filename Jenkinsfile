#!/usr/bin/env groovy

pipeline {

    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                sh 'touch /tmp/t1234.txt'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                
            }
        }
    }
}
