#!/usr/bin/env groovy

pipeline {

    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                touch t1.txt
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                
            }
        }
    }
}
